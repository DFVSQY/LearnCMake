#include "my_math.h"
#include <stdio.h>

int main() {
    printf("visual studio code\n");

    int a = 10, b = 20;
    int c = add_func(a, b);
    int d = sub_func(a, b);
    printf("c:%d d:%d\n", c, d);

    printf("hello world\n");

    return 0;
}